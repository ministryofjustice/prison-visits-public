apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: prison-visits-public
  namespace: prison-visits-booking-staging
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: https://public-staging.pvb.dsd.io
    http:
      paths:
      - path: /
        backend:
          serviceName: prison-visits-public
          servicePort: 3000